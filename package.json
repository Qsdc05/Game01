{
  "name": "folding-museum",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc -b && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "worker:typecheck": "tsc -p worker/tsconfig.json --noEmit",
    "check": "npm run typecheck && npm run worker:typecheck && npm test && npm run build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "jsdom": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest",
    "vitest": "latest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260808.1",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "wrangler": "^4.28.0"
  }
}
